{
  "name": "ride-booking-app",
  "private": true,
  "workspaces": [
    "shared-ui",
    "rider-app",
    "driver-app",
    "admin-dashboard",
    "backend"
  ],
  "scripts": {
    "dev:rider": "npm run dev --workspace=rider-app",
    "dev:driver": "npm run dev --workspace=driver-app",
    "dev:admin": "npm run dev --workspace=admin-dashboard",
    "dev:backend": "npm run dev --workspace=backend"
  }
}
